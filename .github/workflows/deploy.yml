name: Deploy wedding site to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build (подстановка токена и id получателей из Secrets)
        run: |
          mkdir -p build
          rsync -a --exclude=build --exclude=.git --exclude=.github ./ build/

          sed -i "s|__VK_TOKEN__|${{ secrets.VK_ACCESS_TOKEN }}|g" build/index.html
          sed -i "s|__VK_USER_ID_1__|${{ secrets.VK_USER_ID_1 }}|g" build/index.html
          sed -i "s|__VK_USER_ID_2__|${{ secrets.VK_USER_ID_2 }}|g" build/index.html

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: build

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
